import 'package:flutter/material.dart';

class AuthProvider with ChangeNotifier {
//  String _token = "";
//
//  String get getToken => _token;
//
//  final ApiManager mApiManager = ApiManager();
//
//  void login(userName, password) {
//    mApiManager.login(userName, password).then(
//      (onValue) {
//        _token = onValue.data.token;
//        this.notifyListeners();
//      },
//    ).catchError(
//      (onError) {
//        if (onError is ErrorModel) {
//          _token = onError.error;
//        }
//      },
//    );
//  }
}
